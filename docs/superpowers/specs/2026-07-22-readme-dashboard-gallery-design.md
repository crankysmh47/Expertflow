# README Dashboard Gallery Design

Replace the README's five mismatched explanatory SVGs with two real captures of the shipped dashboard. Keep the logo, measured copy, commands, and technical documentation intact. The first capture shows the headline result; the second shows the execution boundary and placement story. Add a compact judge-links block for the repository, product guide, dashboard, and deployment guide. Store optimized PNGs under `docs/assets/`, add alt text, package them through the existing assets allowlist, and verify the README no longer embeds the retired visuals.

