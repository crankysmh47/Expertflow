# Q6 Quality-Preserving Evidence

This directory contains the immutable manifests, compact results, reports, and append-only command ledger for Option 1. Dataset payloads, model weights, binaries, and raw run logs remain outside git; their absolute paths, sizes, and SHA-256 hashes are recorded in the frozen manifest.

The protected exactness rollback floor is ExpertFlow commit `378ce57` on `codex/q6-runtime-final`. No evidence in this directory changes or weakens that historical result.

